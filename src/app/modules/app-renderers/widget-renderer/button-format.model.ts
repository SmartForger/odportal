export interface ButtonFormat{
    green: {
        class: string;
        disabled: boolean;
    };

    yellow: {
        class: string;
        disabled: boolean;
    };

    red: {
        class: string;
        disabled: boolean;
    };
}